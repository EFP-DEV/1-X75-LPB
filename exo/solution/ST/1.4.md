# Exercice 1.4

## Introduction

Dans cet exercice, tu dois **suivre l’algorithme ligne par ligne** et observer comment les variables changent.

Une variable est comme une **boîte** :

* quand on fait une **affectation** (`<-`), on met une valeur dans la boîte ;
* si on affecte une nouvelle valeur plus tard, **on remplace** l’ancienne.

---

## Objectifs

Comprendre que :

* une variable contient une valeur ;
* une affectation remplace la valeur précédente ;
* une variable n’est pas reliée automatiquement à une autre.

---

## Pseudo-code

```
1   Variables cafe, lait, tasse en Entier
2
3   Début
4       cafe <- 3
5       lait <- 10
6       tasse <- cafe + lait
7       lait <- cafe + lait
8       cafe <- tasse
9   Fin
```

---

## Syntaxe

* **Déclaration** : `Variables x, y en Entier`
  → On annonce les variables qu’on va utiliser, et leur type (ici : nombres entiers).

* **Affectation** : `x <- 5`
  → La flèche **<-** se lit **« prend la valeur »** / **« on met dans »**.
  Ce n’est **pas** une égalité mathématique : c’est une **commande** qui remplace la valeur précédente.

* **Utiliser une variable dans un calcul** : `y <- x + 3`
  → On prend la **valeur actuelle** de `x`, on calcule, puis on stocke le résultat dans `y`.

* **Ordre des lignes** : on exécute **de haut en bas**, une ligne après l’autre.

* **Fin de ligne** : dans ce pseudo-code, pas besoin de `;` (contrairement au C).
  Les blocs sont indiqués par `Début` / `Fin` et par l’indentation.

* **Nom des variables** : un nom est une “étiquette” (ex. `cafe`, `lait`, `tasse`) ; il sert à retrouver la valeur stockée.

---

## Lecture en français

| Ligne | Action décrite                                                                  |
| ----: | ------------------------------------------------------------------------------- |
|     1 | Je déclare trois variables entières : `cafe`, `lait` et `tasse`.                |
|     2 |                                                                                 |
|     3 | Je commence l’algorithme.                                                       |
|     4 | J’affecte la valeur 3 à la variable `cafe`.                                     |
|     5 | J’affecte la valeur 10 à la variable `lait`.                                    |
|     6 | Je calcule `cafe + lait`, puis je range le résultat dans `tasse`.               |
|     7 | Je calcule `cafe + lait` (avec les valeurs actuelles), puis je remplace `lait`. |
|     8 | J’affecte à `cafe` la valeur contenue dans `tasse` (je remplace `cafe`).        |
|     9 | Je termine l’algorithme.                                                        |

---

## Inspection des valeurs

| Ligne | Code                                  | cafe | lait | tasse |
| ----: | ------------------------------------- | ---: | ---: | ----: |
|     1 | Variables cafe, lait, tasse en Entier |    — |    — |     — |
|     2 |                                       |    — |    — |     — |
|     3 | Début                                 |    — |    — |     — |
|     4 | cafe <- 3                             |    3 |    — |     — |
|     5 | lait <- 10                            |    3 |   10 |     — |
|     6 | tasse <- cafe + lait                  |    3 |   10 |    13 |
|     7 | lait <- cafe + lait                   |    3 |   13 |    13 |
|     8 | cafe <- tasse                         |   13 |   13 |    13 |
|     9 | Fin                                   |   13 |   13 |    13 |

---

## Idée essentielle

Quand on écrit :

```
lait <- cafe + lait
```

L’ordinateur :

1. regarde les valeurs actuelles de `cafe` et `lait` ;
2. fait le calcul ;
3. range le résultat dans `lait`.

Puis c’est terminé :
il ne garde pas de “connexion” avec les anciennes valeurs.

---

## Image mentale

Une variable n’est pas un tuyau relié à une autre variable.
C’est une boîte indépendante.

Si on change le contenu d’une boîte, cela ne modifie pas les autres boîtes (sauf si on recalcule et qu’on réécrit une valeur).

---

## Conclusion

Après exécution complète :

```
cafe = 13
lait = 13
tasse = 13
```

**Parce que :**

* `tasse` vaut 13 à la ligne 6 (3 + 10) ;
* `lait` devient 13 à la ligne 7 (3 + 10, car `lait` valait encore 10 à ce moment-là) ;
* puis `cafe` prend la valeur de `tasse` à la ligne 8, donc 13.
